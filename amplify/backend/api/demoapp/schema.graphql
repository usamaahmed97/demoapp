type Department @model
 @auth(rules: [{ allow: owner, operations: [create, delete, update]}])
 {
  id: ID!
  name: String!
  manager: Employee @hasOne
}


type Employee @model {
  id: ID!
  name: String!
  age: Int
}

